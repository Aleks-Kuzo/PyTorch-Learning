# PyTorch Learning

Ноутбуки по изучению PyTorch. Каждый файл — отдельная тема: краткая теория и рабочий код с проверками.

## Содержание

| # | Ноутбук | Тема |
|---|---------|------|
| 1 | [`PyTorch1.ipynb`](PyTorch1.ipynb) | Подготовка данных: `Dataset`, `DataLoader`, трансформации |
| 2 | [`PyTorch2.ipynb`](PyTorch2.ipynb) | Первые нейросети: `nn.Module` и цикл обучения |
| 3 | [`PyTorch3.ipynb`](PyTorch3.ipynb) | LR Scheduler'ы, сохранение и загрузка модели |
| 4 | [`PyTorch4.ipynb`](PyTorch4.ipynb) | Ранняя остановка обучения, анализ предсказаний |

## Описание ноутбуков

### 1. PyTorch1 — подготовка данных
Как превратить файлы с диска в батчи тензоров. Задачи: классификация (MNIST по папкам) и регрессия (синтетические картинки с квадратом, предсказание координат центра). Разбираются собственные `Dataset` и `ImageFolder`, `random_split`, `DataLoader`, трансформации `transforms` / `transforms.v2` и собственная трансформация через `torch.nn.Module`.

### 2. PyTorch2 — первые нейросети и цикл обучения
Построение моделей и их обучение на данных из первого ноутбука. Способы задать модель: `nn.Sequential`, собственный класс `nn.Module`, `ModuleList` / `ModuleDict`, модели с несколькими входами и выходами. Далее полный цикл обучения (прямой проход, ошибка, `backward`, шаг оптимизатора, валидация, метрики, графики loss/accuracy) на двух задачах: классификация MNIST (`CrossEntropyLoss`, Adam) и регрессия координат (`MSELoss`). Перенос модели и данных на GPU.

### 3. PyTorch3 — LR Scheduler'ы, сохранение и загрузка модели
Планировщики скорости обучения: `LambdaLR`, `MultiStepLR`, `StepLR`, `ExponentialLR`, `ReduceLROnPlateau` — с графиками изменения lr. Применение шедулера в цикле обучения на задаче регрессии координат. Сохранение и загрузка модели через `state_dict` (`torch.save` / `torch.load`), а также чекпоинт с полным состоянием (модель, оптимизатор, шедулер, история loss/метрик) для дообучения с прерванной эпохи.

### 4. PyTorch4 — ранняя остановка и анализ предсказаний
Полный цикл обучения с ранней остановкой (`EarlyStopping`) на двух задачах: классификация MNIST и регрессия координат. Собственный класс `EarlyStopping`, отслеживающий метрику и прекращающий обучение при отсутствии улучшений. Сохранение чекпоинта лучшей эпохи и загрузка модели для финальной проверки на тесте. Разбор предсказаний: визуализация верных и ошибочных ответов модели на изображениях.

## Установка

```bash
git clone https://github.com/Aleks-Kuzo/<repo-name>.git
cd <repo-name>
pip install torch torchvision numpy matplotlib pillow jupyter
```

Python 3.9+, `torchvision` ≥ 0.16. В ноутбуках используются локальные пути к данным — замените их на свои.
